name: Nuclei Vulnerability Scan Called Workflow

on:
  workflow_call:
    inputs:
      urls_file:
        required: true
        type: string

jobs:
  nuclei-scan:
    name: Scan
    runs-on: ubuntu-latest
    steps:
      # GitHub Checkout
      # https://github.com/marketplace/actions/checkout

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      # Nuclei
      # https://github.com/projectdiscovery/nuclei-action

      - name: Nuclei vulnerability scan
        id: nuclei_scan
        uses: projectdiscovery/nuclei-action@7125d52fb64241bfd755b06dadcd3075192da010  # v3
        with:
          args: "-list ${{ inputs.urls_file }} -severity low,medium,high,critical -H 'User-Agent: Nuclei - Vulnerability Scan (osinfra.io)' -o nuclei.log -sarif-export nuclei.sarif"

      # GitHub Upload Artifact
      # https://github.com/actions/upload-artifact

      - name: GitHub workflow artifacts
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f  # v7.0.0
        with:
          name: nuclei-artifacts
          path: |
            nuclei.log
            nuclei.sarif

      # GitHub CodeQL upload SARIF
      # https://github.com/github/codeql-action

      - name: GitHub security dashboard alerts update
        uses: github/codeql-action/upload-sarif@0d579ffd059c29b07949a3cce3983f0780820c98  # v4.32.6
        if: success() && hashFiles('nuclei.sarif') != ''
        with:
          sarif_file: nuclei.sarif
          category: Nuclei
